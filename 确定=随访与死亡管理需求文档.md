
---

## 1. 文档目的与范围

---

## 5. 随访信息管理

### 5.1 页面布局

- 顶部 `page-toolbar`：**模板下载 | 批量上传**（与死亡信息列表「批量登记」入口布局一致，不塞进筛选行）。
- 筛选行右侧 `filter-actions`：仅 **重置**。
- 筛选下方为记录统计与列表。

### 5.2 筛选项

| 筛选项    | 说明                                    |
| ------ | ------------------------------------- |
| 行政区划   | 省市区级联                                 |
| 校验状态   | 全部 / 未校验 / 错误 / 警告 / 通过 / 不校验         |
| 最后接触状态 | 全部 / 1 存活 / 2 死亡 / 3 移居 / 4 失访 / 9 不详 |
| 报卡类型   | 全部 / 原始卡 / 补报卡 / 合并卡                  |
| 检索     | 报告卡编号 / 姓名 / 身份证号（关键字）                |

### 5.3 列表字段

操作列：**查看 | 添加**（编号点击等同查看）。

- **查看**：进入整页「查看随访记录」，**随访记录历史列表，列表字段同登记字段**。查看快捷添加入口
- **添加**：进入与报告卡登记第三步相同的整页「随访报告信息」表单（顶部三步条停在第 3 步）；保存后追加接触事件并刷新档案当前状态；结局=死亡时同步死亡列表。校验见 **5.3.1**。保存行为为**追加一条接触事件**并刷新档案当前状态


**错误（阻断，须修正后方可保存/入库）**

| 规则                                                | 说明                                      |
| ------------------------------------------------- | --------------------------------------- |
| 最后接触日期必填，且格式须为 `yyyy-MM-dd`                       | 无条件必填；批量文本须显式校验格式，单条表单由日期控件约束           |
| 最后接触状态必填，且须在枚举内（1 存活 / 2 死亡 / 3 移居 / 4 失访 / 9 不详） | 见 §3.3                                  |
| 状态=死亡时，死亡地点、根本死因、死因 ICD10、死亡日期条件必填                | **条件必填**；非死亡状态须自动清空上述死亡字段（与报告卡 §8.2 一致） |
| 当前提交的最后接触日期不得早于该档案历史最后接触日期                        | 不得早于历史列表最新一条接触日期                        |
| 死亡日期不得早于确诊日期                                      | 有确诊日期时校验                                |
| 有死亡日期时，最后接触日期不得晚于死亡日期                             | 与报告卡 §8.3「不得晚于」一致                       |
| 根本死因为肿瘤时，死因 ICD10 须以 C 或 D 开头                     | 与报告卡 §8.3、本模块 §7.4 一致                   |

**警告（可保存/入库，须人工确认）**

| 规则             | 说明              |
| -------------- | --------------- |
| 最后接触日期早于确诊日期   | 弹窗提示，确认后可保存/入库  |
| 死亡日期与最后接触日期不一致 | 弹窗提示人工确认后可保存/入库 |

**边界说明**：随访批量**不携带诊断依据**；报告卡 §8.3 的 DCO / 非 DCO 日期等式在报告卡登记、死因建卡侧校验，不在此处强制。死亡日期不一致按上表「警告」处理即可，待报告卡/死因侧补全诊断依据后再行严格校验。

### 5.4 批量上传

交互对齐报告卡「报告卡登记 / 批量登记」：整页临时队列 + 小弹窗选文件，**不再使用大弹窗展示校验结果**。

#### 5.4.1 流程

```text
随访信息上传页点击「批量上传」
  → 进入整页「随访批量上传」并自动打开选文件弹窗
  → 「确定添加」后解析结果落入整页列表（含表头；校验状态 / 存在状态）
  → 行操作「编辑」：进入与随访「添加」相同的整页「随访报告信息」表单，保存后写回临时行并重新校验
  → 勾选通过/警告行 →「批量上传」入库随访主表
  → 错误 / 不存在行可单条或批量删除（亦可先编辑修正后再入库）
```

#### 5.4.2 整页信息

- 筛选：行政区划、存在状态、校验状态、检索； **重置**
- 勾选后批量条：**导出 Excel | 批量上传 | 批量删除**
- 列表**始终带表头**（无数据时表体内提示并可再次「选择文件」）
- 列表列：
  | 校验状态 | 存在状态 | 姓名 | 身份证号 | 最后接触日期 | 最后接触状态 | 死亡日期 | 根本死因 | 死因ICD10 | 死亡地点 | 报告医生 | 上传日期 | 操作 |
  | ---- | ---- | -- | ---- | ------ | ------ | ---- | ---- | ------- | ---- | ---- | ---- | -- |
- 操作列：**编辑 | 删除**
  - **编辑**：跳转地址与随访信息「添加」相同（报告卡第三步同款「随访报告信息」整页）；预填本行字段；保存后写回临时队列该行并按 §5.3.1 / §5.4.3 重新校验，返回批量上传整页（**不直接入库主表**）。
  - **删除**：从临时队列移除该行。
- 选文件弹窗：上传区 + 取消 / 确定添加（同报告卡批量登记弹窗）

#### 5.4.3 校验与入库策略

- 系统中无匹配随访主记录（按**姓名 + 身份证号**联合匹配）时，列表「校验状态 / 存在状态」均体现「错误不存在」。
- 「不存在」行**不可直接入库**。

**匹配与写入（命中已有记录）**

- 匹配键：姓名 + 身份证号联合匹配已有随访主记录。
- 命中**单张**报告卡/随访行：直接更新该行。
- 命中**多张**报告卡（多原发）：按 §9.4 / §9.5 人级传播规则，**全灶更新**最后接触、结局、来源=批量导入、上传日期及死亡相关字段；禁止只更新匹配到的第一行。

**入库策略**

- **通过 / 警告**：可确认入库；含警告行时须在「确认入库」动作中视为**人工确认**；写入后校验状态可为「未校验」或业务约定值，来源=批量导入。
- **错误 / 不存在**：不可入库；用户可删除该行后对其余行确认入库，或改模板后重传。
- **死亡字段条件写入**：仅当 State=2(死亡) 时写入死亡日期、根本死因、死因 ICD10、死亡地点；非死亡状态须清空死亡相关字段（与 §5.3.1 一致）。
- **可见性**：入库成功后随访主表即时更新；若该行 status=死亡，死亡信息列表（§7）即时可见（见 §9.2 时效）。
- 数据入库后需同步更新报告卡的随访状态

### 5.5 数据来源（继承老业务三路径）

| 路径      | 说明             |
| ------- | -------------- |
| 报告卡登记录入 | 第三 Tab 同步创建/更新 |
| 随访批量上传  | 本节 5.4         |
| 死因匹配回写  | 见第 8、9 章       |

---

## 6. 随访周期管理

### 6.1 定位

按**癌种统一维护「随访间隔期（月）」**的配置入口：登记在册患者按诊断癌种套用对应间隔期，周期性地生成应随访任务。默认为12个月，可自定义。

### 6.2 癌种来源约束（硬规则）

- 随访周期**只能挂接系统肿瘤登记癌种目录**即字典维护的癌种目录，**不允许自定义录入目录外癌种，若字典新增则自动同步**。
- 唯一性：每个癌种（按 ICD-10 编码）**仅允许一条**周期配置。

### 6.3 筛选项

| 筛选项 | 说明 |
| ---- | --------------------- |
| 搜索   | 关键字匹配 癌种名称 / ICD-10 编码，大小写不敏感，回车触发 |
| 状态   | 全部 / 启用 / 停用        |

### 6.4 列表字段

| 列      | 说明                                                    |
| ------ | ----------------------------------------------------- |
| 癌种     | 癌种名称（字典目录带入，不可手改）                                     |
| ICD-10 | 癌种编码，唯一键                                              |
| 随访间隔期  | **行内数字输入框**，单位「月」，范围 1–60，失焦即保存生效；停用行输入框置灰不可编辑        |
| 状态     | 启用(绿) / 停用(灰) 徽标                                      |
| 操作     | 启用 ↔ 停用（互斥切换）                                         |

### 6.5 间隔期维护

- 修改输入框值即校验：**1–60 整数通过**并保存，提示「{癌种} 随访间隔期已设为 N 个月」。
- 非法值（<1、>60、非数字、清空）：该行间隔置为**空（待配置）**，警示提示「{癌种} 随访间隔期待配置」。
- **间隔为空的启用行不允许停用**：点停用时报错「{癌种} 尚未填写随访间隔期」，须先补间隔再停用（防止停用后输入框 disabled 把行冻结在未配置状态）；停用行可直接启用，启用后补填间隔。

### 6.6 启用 / 停用

- 行操作即时切换启用 ↔ 停用。
- 停用的癌种**不参与后续周期性随访任务生成**；已生成的存量待办任务不追溯调整。
- 本页面**不提供添加 / 删除 / 导出 / 重置入口**；配置行由癌种目录同步产生（见 §6.2），仅维护间隔期与启停状态。



---

## 7. 死亡信息列表

### 7.2 页面布局与操作

- 顶部 ：**&#x20;批量登记模板下载 |**
  - **模板下载**：死因批量模板。
  - **批量登记**：进入第 8 章临时表整页，并自动打开选文件弹窗（交互对齐随访「批量上传」）。
- 筛选：行政区划、校验状态、死因、日期类型+区间、检索。
- 筛选行右侧：**重置 | 导出 Excel**。

### 7.3 列表字段

| 字段：                                |
| ---------------------------------------- |
| 序号、校验状态、报告卡编号、姓名、性别、年龄、诊断日期、ICD10             |
| 最后接触、结局、死亡日期、根本死因、死因 ICD、死亡地点、生存月数、操作 |
                               |


### 7.4 数据如何进入本表

本表**不**因「临时表上传」自动增行。可见来源仅：

1. 随访/报卡将结局改为死亡并保存（见 §9.2）
2. 死因临时表入库后，**匹配回写成功**（见 §8.5、§9.3）
3. 无报告卡时按 §8.6 自动/补全建卡并回写
4. 数据来源：报告卡列表中的非死亡人员

---

## 8. 死因批量登记

### 8.1 流程
```text
死亡信息列表「批量登记」
  → 临时表整页 + 自动打开选文件弹窗
  → 解析落入临时表（校验 / 编辑 / 删除）
  → 勾选「通过」行「批量入库」→ 死因匹配池（未匹配）
  → 匹配回写成功 → 第 7 章死亡信息列表可见
```
硬约束：**入库到匹配池 ≠ 进入死亡信息列表**；禁止把临时表行直接当作第 7 章列表行展示。

### 8.2 页面布局

- 筛选：行政区、校验状态、日期、检索；右侧仅 **重置**。
- 勾选后批量条：**导出 Excel | 批量入库 | 批量删除**。
- 列表始终带表头；无数据时可「选择文件」再传。

### 8.3 临时表字段与行操作

| 列       | 说明                                                          |
| ------- | ----------------------------------------------------------- |
| 校验状态    | 通过 / 警告 / 错误/                                               |
| 存在状态    | 系统是否已有对应/报告卡（存在 / 不存在）                                    |
| 人口与死亡字段 | 姓名、身份证、性别、出生日期、最后接触日期、死亡日期、根本死因、死因 ICD、死亡地点、报告单位、上传时间、错误说明等 |
| 操作      | \*\*编辑，删除                                                   |

- **编辑**：进入与随访「添加」相同的「随访报告信息」整页；保存写回**本临时行**并重校验，不直接写第 7主表。
- **删除**：从临时表移除该行。

### 8.4 入站校验

模板支持 `.xls / .xlsx / .csv`。入站校验分两步：**先重卡校验（判定是否在册），再按在册结局决定通过条件**；通用字段规则仍见 §5.3.1 及报告卡文档。

**第一步：重卡校验（是否在册）**

- 按匹配库内报告卡：
  - **命中（有重卡）**：系统已有该人记录 → 存在状态=存在，进入第二步结局判定。
  - **未命中（无重卡）**：库内无该人 → 存在状态=不存在，直接转入 **§8.6 建卡流程**（须校验肿瘤信息是否完备），不在此判定「通过」。

**第二步：在册行的结局判定（仅对命中行）**

- 重卡校验通过 **且** 该人当前结局=未死亡：校验 **通过**。此类行**不重复校验肿瘤信息**（匹配卡已含肿瘤字段），仅用于**更新随访结局为死亡**及相关死亡字段。但上传数据为未死亡时报错误
- 重卡校验通过 **但** 该人当前结局=已死亡（重复死亡记录）：标记 **警告 / 重复**。
- 其余（必填缺失、ICD 不合规等）：按 §5.3.1 公共规则判 **错误**。

**入库策略（基于以上校验状态）**

- **通过**：可勾选批量入库至匹配池 → 走 §8.5 匹配回写。
- **警告**：须人工确认（含重复死亡、死亡日期不一致等）；确认后可入库。
- **错误 / 不存在且不可建卡路径未走通**：不可入库；可编辑修正或删除后重传。
- 通过/不存在：自动新建卡

### 8.5 入库与匹配回写

1. **批量入库**：仅把临时表通过行写入死因匹配池。
2. **匹配回写**：按重卡规则匹配，有多原发同步更新多原发卡随访信息；成功后回写随访主表死亡字段，`source=死因回写`，第 7 章自动可见。
3. 多卡命中、回写字段、不可逆等细则见 **§9.3**。

### 8.6 无报告卡时的建卡

临时表/匹配池中按身份证在报告卡与随访主表均无命中时，，须进入建卡通道：

| 存在状态 | 建卡完备度 | 列表标记          | 系统行为                      |
| ---- | ----- | ------------- | ------------------------- |
| 不存在  | 信息不完备 | `待补全建卡`       | 不可自动建卡；提供「补全并建卡」/编辑进入报卡补全 |
| 不存在  | 信息完备  | `可自动建卡` 或直接执行 | **自动生成报告卡**及死亡随访行，再匹配回写   |
|              |

**信息完备**：符合报告卡上传校验（与报告卡模块约定一致）。

**不完备**：标记错误/待补全 → 编辑携带数据进报卡页 → 校验通过后生成报告卡 → 生成/更新随访死亡行 → 关联报告卡编号、匹配状态=已匹配 → **第 7 章可见**。

**完备自动建卡**：可批量汇总提示「自动建卡 N 条」；须写审计（操作者、时间、源死因编号、生成报告卡编号）。来源类型=死因补建（与报告卡模块枚举约定）。

---

## 9. 关键同步与多原发规则



### 9.2 随访登记了死亡 → 如何同步到死亡列表

```text
触发源（任一）：
  A. 报告卡登记/编辑「随访报告信息」Tab，最后接触状态=死亡并保存
  B. 随访信息上传中编辑保存，结局改为死亡
  C. 随访批量上传确认入库，行结局=死亡
  D. 死亡信息列表内编辑（改的是同一条主表）

处理：
  1. 写随访主表该报告卡行：status=死亡，死亡日期/地点/死因/ICD/最后接触日期等
  2. 执行「人级死亡传播」（见 9.4）：同身份证下其他存活原发灶行同步死亡字段
  3. 关闭该人全部未完成随访任务（见 9.5）
  4. 同步报告卡档案只读聚合（reportCardFollowup）
  5. 死亡信息列表查询条件为 status=死亡 → 刷新后立即可见，无需单独「推送到死亡库」
  6. 补充：若删除死亡状态，则将死亡状态恢复为存活
```

| 项     | 规则                                                                                                                                                                      |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 可见性   | `死亡信息列表` ≡ `随访主表 WHERE 最后接触状态=2(死亡)`；同一物理行                                                                                                                              |
| 时效    | 保存成功后即时；列表下次查询/刷新即见，禁止异步隔夜同步作为默认                                                                                                                                        |
| 必填    | 状态=2(死亡)时：死亡日期、死亡地点、根本死因、死因 ICD10、最后接触日期必填；缺项则校验错误，仍可出现在列表但校验状态≠通过（错误/不校验）                                                                                              |
| 来源    | 报卡录入 / 死因录入 / 随访等，按实际操作写入 `source`                                                                                                                                    |
| 直接改状态 | 允许通过随访直接修改最后接触状态（如死亡→存活/失访/移居等）。若改回非死亡：该行移出死亡列表；人级是否连带其他灶 → 须二次确认「是否恢复该患者全部原发灶为非死亡」（默认需确认，禁止静默）。**来源门控**：若该死亡记录 `source=死因回写`，改回非死亡须先走死因侧专项解锁权限（见 9.3 不可逆），不得仅凭本确认即翻转不可逆匹配。 |
                                                                        |

### 9.4 多原发：死亡如何在多灶之间传播

```text
患者身份证 S 下存在报告卡/随访行：卡A（肺）、卡B（胃）均为存活
任一通道将「人 S」登记为死亡（随访编辑 / 报卡 / 死因匹配 / 批量导入）
  → 卡A、卡B 的最后接触状态均改为死亡
  → 死亡日期、死亡地点、根本死因、死因 ICD、最后接触日期：人级字段写一致
  → 各灶生存月数仍按「本卡确诊日 → 死亡日」分别计算
  → 死亡信息列表出现 A、B 两行（同一人、两个原发灶），便于按部位统计与质控
```

| 规则    | 说明                                            |
| ----- | --------------------------------------------- |
| 人死灶齐  | 禁止只死一个原发灶、另一灶仍「存活」参与发病后随访（避免统计与任务冲突）          |
|      |
|      |
|  |


### 9.8 与报告卡作废 / 恢复的联动

《报告卡管理需求文档》4.4 允许作废 / 恢复报告卡。本模块消费报告卡死亡结局，须定义级联，避免「死人卡被作废但死亡列表仍可见」或「死因侧已匹配但卡没了」的脏状态。


| 动作               | 本模块联动规则                                                                                                                      |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 作废带死亡结局的报告卡      | ① 该卡对应随访主行：若人级仍仅此灶为死亡，则移出死亡列表可见范围；② 暂停生存月数计算；③ 死因侧若曾「已匹配」回写，回退为「待复核」（不静默丢弃，保留审计）；④ 若人级其他灶仍为死亡，保持死亡列表可见，仅移除本卡行。 |
| 恢复被作废的死亡卡        | 反向恢复：死亡列表重新可见、生存月数恢复计算、死因匹配态回退为「已匹配」。                                                                                        |
| 仅作废非死亡卡 / 恢复非死亡卡 | 不影响死亡列表与死因匹配态。                                                                                                               |

**约束**：级联须与报告卡模块同事务或最终一致，并写审计（操作者、时间、源卡编号、联动影响行）。人级死亡传播（9.4）在级联后仍须保持「人死灶齐」——若所有灶均被作废/改非死亡，则死亡列表不再出现该人。

---

##

---



---
